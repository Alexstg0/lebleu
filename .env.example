# Cadena de conexión a Postgres (Neon en producción).
# Local: copia este archivo a .env y pega tu DATABASE_URL de Neon.
DATABASE_URL="postgresql://usuario:password@host.neon.tech/dbname?sslmode=require"
